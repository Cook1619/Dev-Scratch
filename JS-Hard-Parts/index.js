// declaring a variable called num and assigning 3 to it
const num = 3;

// declaring a function with one input, then declares a variable named result which is the input times 2, then returning result
function multiplyBy2(input){
    const result = input * 2;
    return result;
}